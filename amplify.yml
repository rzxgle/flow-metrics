version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --omit=dev
    build:
      commands:
        - npm run build:amplify
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
